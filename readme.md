# Cloning in project
Go to your terminal window, and type in `git clone` and the ssh url under code at the top of the github repository (green button) e.g `git@github.com:rshanks379/Work-Experience.git`

# Troubleshooting

# I don't have git

You can either use this https://desktop.github.com/download/ or you can use the terminal. For this install using https://git-scm.com/install/windows and potentially use a client like putty or windows terminal?

# General Notes

Dates are read YYYY/MM/DD.<br/>
To use a seeder you need a factory first.<br/>
second part of my return view wants to edit the file.<br/>
A double quote is normally used so you dont confuse when using the plural of a word.<br/>
Foreloop is for a known number, Foreach is for unknown.<br/>
when creating a form you need to link the route to the data on the views part.<br/>
Pivot tables summarise large amounts of data into smaller and summary tables for specific questions

# Remember the functions of these codes 

`","` used to seperate the the answers of the data.<br/>
`php artisan make:` to Create a new view ext.<br/>
`@` is to include lines from other views<br/> 
`$` means a variable<br/>
`<br>` is used to make a break onto different lines<br/> 
`<select>` used for making drop down lists<br/> 
`[]` brackets mean an array<br/> 
`{}` groups multiple lines into one block and define the scope of variables <br/>
`<>` creates tags to label and format letters 

# When Using a Migration Follow This 

outside class = function<br/>
inside = method<br/>
String-String of letters<br/> 
Binary-String of numbers<br/> 
Boolean-Either true or false = e.g 1 or 0

# Model View Controller 

Model - Defines data<br/> 
View - Defines UI<br/>
Controller - contains control logic 