# Cloning in project
Go to your terminal window, and type in `git clone` and the ssh url under code at the top of the github repository (green button) e.g `git@github.com:rshanks379/Work-Experience.git`

# Troubleshooting

# I don't have git

You can either use this https://desktop.github.com/download/ or you can use the terminal. For this install using https://git-scm.com/install/windows and potentially use a client like putty or windows terminal?

# General Notes

Dates are read YYYY/MM/DD
To use a seeder you need a factory first 
second part of my return view wants to edit the file 
A double quote is normally used so you dont confuse when using the plural of a word 
Foreloop is for a known number, Foreach is for unknown 
when creating a form you need to link the route to the data on the views part 
Pivot tables summarise large amounts of data into smaller and summary tables for specific questions 

# Remember the functions of these codes 

`","` used to seperate the the answers of the data 
`php artisan make:` to Create a new view ext.
`@` is to include lines from other views 
`$` means a variable 
`<br>` is used to make a break onto different lines 
`<select>` used for making drop down lists 
`[]` brackets mean an array 
`{}` groups multiple lines into one block and define the scope of variables 
`<>` creates tags to label and format letters 

# When Using a Migration Follow This 

outside class = function 
inside = method 
String-String of letters 
Binary-String of numbers 
Boolean-Either true or false = e.g 1 or 0 

# Model View Controller 

Model - Defines data 
View - Defines UI
Controller - contains control logic 